TASK:

1.  Create a word-guessing game, which should have:
  1.1. Both static and dynamic polymorphisms.
  1.2. All types of inheritance (3 of them).
  1.3. At least one interface.
  1.4. At least one abstract class.
2. The game should have intro (i.e. your full name, when program was created), as well as at least one drawing (person who will be hanged if player will use up all options) and basic rules (how many tries will have (and also should able to set game difficulty at which will change how many guesses player will have), how difficult (long) word will be, etc.).
3.  OPTIONAL: Make how-to play as well with introductionary gameplay demonstration (can be either automatic or manual).
